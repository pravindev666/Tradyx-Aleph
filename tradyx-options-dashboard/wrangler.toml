# Cloudflare Pages Configuration (optional)
# Cloudflare Pages auto-detects Next.js, but this provides explicit config

name = "tradyx-dashboard"
compatibility_date = "2024-01-01"

[env.production]
  name = "tradyx-dashboard"

# Build configuration (if using Cloudflare Pages build)
# Note: We recommend building in GitHub Actions and deploying pre-built files
# [build]
#   command = "npm run build"
#   cwd = "tradyx-options-dashboard"

